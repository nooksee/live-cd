#!/usr/bin/env python3
"""Console entrypoint for the LiveCD Creator backend CLI."""

import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.abspath(__file__)), os.pardir))

from livecd.cli import main

if __name__ == "__main__":
    sys.exit(main())
