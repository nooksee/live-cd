#!/usr/bin/env python3
"""Console entrypoint for the LiveUSB Creator backend CLI."""

import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.abspath(__file__)), os.pardir))

from liveusb.cli import main

if __name__ == "__main__":
    sys.exit(main())
